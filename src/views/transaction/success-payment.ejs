<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment</title>
    <%- include('../partials/cdn') %>

<body style="background-color: #eee; min-height: 100vh">
    <div class="container" style="height: 100vh;">
        <div class="row justify-content-center align-items-center h-100">
            <div class="col-12 col-sm-10 col-lg-6 col-xl-5">
                <div class="rounded-4 bg-white shadow p-4 my-2">
                    <p class="text-center text-success fs-1 mb-3">
                        <i class="fa-regular fa-circle-check"></i>
                    </p>
                    <h2 class="text-center text-success mb-0">Payment Success</h2>

                    <div class="px-3 mt-5">
                        <h4>Payment detail</h4>

                        <div class="row justify-content-between">
                            <div class="col-6"><span class="fw-semibold">From:</span></div>
                            <div class="col-6 text-end fw-light">
                                <%= name %>
                            </div>
                        </div>

                        <div class="row justify-content-between">
                            <div class="col-6"><span class="fw-semibold">To:</span></div>
                            <div class="col-6 text-end fw-light">
                                <%= clientName %>
                            </div>
                        </div>

                        <div class="row justify-content-between">
                            <div class="col-6"><span class="fw-semibold">Time:</span></div>
                            <div class="col-6 text-end fw-light">
                                <%= date %>
                            </div>
                        </div>

                        <div class="row justify-content-between">
                            <div class="col-6"><span class="fw-semibold">Transaction ID:</span></div>
                            <div class="col-6 text-end fw-light">
                                <%= transactionID %>
                            </div>
                        </div>

                        <div class="row justify-content-between align-items-center mt-4">
                            <div class="col-6"><span class="fw-semibold">Total amount:</span></div>
                            <div class="col-6 text-end fs-4 text-danger fw-bold">$<%= amount %>
                            </div>
                        </div>
                    </div>

                    <div class="text-end px-3 mt-4 mb-2">
                        <a class="btn btn-outline-primary text-uppercase px-5" href="<%= callback_url %>">Back</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>